[pytest]
testpaths = tests
markers =
    e2e: drives the whole stack through containers and subprocesses; slow
    chaos: injects a failure and asserts the detector fires; slow and mutates the source
