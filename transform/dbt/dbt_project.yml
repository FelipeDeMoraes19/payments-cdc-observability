name: payments
version: "1.0.0"
profile: payments

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  payments:
    staging:
      +materialized: view
    marts:
      +materialized: table
